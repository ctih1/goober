exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv",
]

line-length = 94
indent-width = 4

target-version = "py310"

[lint]
ignore=[
    "UP006", # e.g. use dict instead of Dict for typing
    "DTZ001", # specify timezone
    "DTZ005", # specify timezone
    "DTZ003", # don't use datetime.utcnow()
    "DTZ002", # don't use datetime.today()
    "BLE001", # except Exception as
    "LOG015", # logger.critical
    "F401", # untyped libs
    "UP035", # typing.(type) is deprecated, use (type) instead
    "PLR1722", # use sys.exit() instead of quit
]


[format]
quote-style="double"
indent-style="space"
line-ending="lf"
nested-string-quote-style="preferred"
